# Use this for running Prisma migrations
# Copy this to .env.migration and update with your direct connection URL (port 5432)
# Run migrations with: dotenv -e .env.migration -- npx prisma migrate dev

# Direct connection (port 5432) - for migrations
DATABASE_URL="postgresql://user:password@aws-1-us-east-2.pooler.supabase.com:5432/postgres"

# Note: Your regular .env should use port 6543 (pooler) for application queries
